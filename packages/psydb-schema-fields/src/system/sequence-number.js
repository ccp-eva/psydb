'use strict';
var SequenceNumber = () => {
    // FIXME: maybe add pattern, but it is set only by the system itself
    var schema = { type: 'string' };
    return schema;
}

module.exports = SequenceNumber;
