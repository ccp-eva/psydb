'use strict';

module.exports = (bag) => {
    var meta = {
        collection: 'experimentVariant',
        isGenericRecord: true,
        hasCustomTypes: false,
        hasFixedTypes: false,
        hasSubChannels: false,
    };
    return meta;
}

