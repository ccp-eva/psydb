'use strict';

module.exports = (bag) => {
    var meta = {
        collection: 'subjectContactHistory',
        isGenericRecord: false,
        hasCustomTypes: false,
        hasFixedTypes: false,
        hasSubChannels: false,
    };
    return meta;
}

