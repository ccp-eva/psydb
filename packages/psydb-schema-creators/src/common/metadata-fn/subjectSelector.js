'use strict';

module.exports = (bag) => {
    var meta = {
        collection: 'subjectSelector',
        isGenericRecord: true,
        hasCustomTypes: false,
        hasFixedTypes: false,
        hasSubChannels: false,
    };
    return meta;
}

