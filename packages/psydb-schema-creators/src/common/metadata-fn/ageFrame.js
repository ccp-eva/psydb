'use strict';

module.exports = (bag) => {
    var meta = {
        collection: 'ageFrame',
        isGenericRecord: true,
        hasCustomTypes: false,
        hasFixedTypes: false,
        hasSubChannels: false,
    };
    return meta;
}

