import React from 'react';
import classnames from 'classnames';

export const ScalarArrayItemFieldWrapper = (ps) => {
    var { index, hasError, formikMeta } = ps;
    var { error } = formikMeta;
    return (
        <div>
            FOOOOOOOOOOOOOO
        </div>
    )
}
