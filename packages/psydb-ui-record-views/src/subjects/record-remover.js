import React from 'react';

export const RecordRemover = (ps) => {
    var {
        collection,
        recordType,
        id,
        onSuccessfulUpdate
    } = ps;

    return (
        <div>REM</div>
    )
}
