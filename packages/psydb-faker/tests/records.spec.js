'use strict';
var { expect } = require('chai');

describe('records', () => {
    it('does the thing', () => {
        var refcacte = {};
        var order = [
            'systemRole',
            'researchGroup',
            'personnel',
        ];

        for (var collection of order) {
            var refcache[collection] = [];
            var faked = Records[collection]({ refache });
        }
    })
})
